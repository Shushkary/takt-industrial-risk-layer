# Шаблон: готовность к релизу (Release readiness)

Заполните поля и отметьте чеклисты перед выкладкой в целевую среду.  
Продукт: **TAKT Industrial Risk Layer** · Репозиторий: **`takt-industrial-risk-layer`**

---

## 1. Карточка релиза

| Поле | Значение |
|------|----------|
| Номер / тег версии | |
| Дата документа | |
| Ветка / коммит (SHA) | |
| Ответственный за релиз | |
| Целевая среда (prod / preprod / пилот) | |
| Окно выкладки (UTC или локально) | |
| Связанные тикеты / PR | |

**Краткое описание релиза** (1–3 предложения: зачем выкладываем, ключевые изменения для оператора):

<!-- ... -->

**Вне области релиза** (явно исключено):

<!-- ... -->

---

## 2. Функциональная готовность

Отметьте `[x]` / `[ ]` или краткий комментарий.

| Область | Готово | Комментарий |
|---------|--------|-------------|
| Оценка риска / кейсы (`/assess`, карточки) | | |
| Экспорт SIEM / PDF | | |
| Forensic bundle + verify | | |
| Решения оператора / baseline | | |
| Compliance: отчёты, чеклист, remediation workflow | | |
| GosSOPKA экспорт (MVP или production-контракт) | | |
| Импорт/экспорт кейсов (`full.json`) | | |

---

## 3. Нефункциональные требования

### 3.1 Качество и автоматические ворота

| Проверка | Статус | Где зафиксировано |
|----------|--------|-------------------|
| Полный pytest | | локально / CI |
| Import-linter (`lint-imports`) | | CI |
| SBOM CycloneDX | | `scripts/generate_sbom.py`, CI |
| pip-audit | | CI |
| Schemathesis (ограниченный прогон) | | CI |
| Mutation-тесты (согласованный scope) | | CI |

### 3.2 Безопасность и комплаенс

| Тема | Статус | Комментарий |
|------|--------|-------------|
| API-ключ / auth в prod | | |
| Подпись forensic root hash (режимная: `mvp` или `gost_strict`) | | см. переменные `TAKT_FORENSIC_*`; для `gost_strict` требуется внешний signer/verifier |
| Ledger: SQLite append-only + verify | | см. endpoints и CLI в `scripts/` |
| Политика SIEM webhook / allowlist | | |

### 3.3 Наблюдаемость

| Тема | Статус | Комментарий |
|------|--------|-------------|
| `/metrics` включён в нужных средах (`TAKT_METRICS`) | | |
| Импорт dashboard Grafana | | `deploy/monitoring/grafana/` |
| Импорт правил Prometheus → Alertmanager | | `deploy/monitoring/prometheus/` |
| OpenTelemetry (если обязательно по ТЗ) | | |

---

## 4. Данные и миграции

| Шаг | Статус | Комментарий |
|-----|--------|-------------|
| Текущая версия схемы SQLite согласована | | см. health / метаданные БД |
| Выполнен `scripts/db_migrate.py` на всех узлах | | |
| Резервное копирование до выкладки (`scripts/db_backup.py`) | | |
| Откат схемы (если применимо) описан | | |

---

## 5. Конфигурация и секреты

Заполните фактические значения **не в этом файле**, а в secret store / runbook; здесь только ссылки или «настроено / N/A».

| Параметр | Настроено | Заметка |
|----------|-----------|---------|
| `TAKT_CONFIG` / путь к `risk_weights.yaml` | | |
| Storage: memory vs SQLite + путь БД | | |
| `TAKT_FORENSIC_CRYPTO_MODE` (`mvp` / `gost_strict`) | | |
| `TAKT_FORENSIC_HMAC_SECRET` (для `mvp`) | | |
| `TAKT_FORENSIC_SIGN_URL` / `TAKT_FORENSIC_VERIFY_URL` (обязательно для `gost_strict`) | | |
| SIEM URL, ретраи | | |
| Build revision (`TAKT_BUILD_REVISION`) | | |

---

## 6. Развёртывание и откат

| Шаг | Ответственный | Статус |
|-----|---------------|--------|
| Образ / артефакт собран из зафиксированного SHA | | |
| Smoke после выкладки (health, assess, `gossopka_official_ok=true`, `forensic_signing_unavailable=false`, `forensic_verify_ok=true`, `audit_engagement_api_ok=true`) | | |
| План отката (образ + БД / без отката схемы) | | |
| Коммуникация стейкхолдерам | | |

---

## 7. Известные ограничения (release notes)

Перечислите то, что оператор должен знать сразу после релиза:

<!-- Например: ledger verify недоступен при storage=memory; GosSOPKA — внутренний MVP до согласования с оператором. -->

---

## 8. Подпись готовности

| Роль | ФИО | Дата | Подпись / ссылка на тикет |
|------|-----|------|---------------------------|
| Разработка | | | |
| Безопасность (по процессу) | | | N/A |
| Эксплуатация | | | |
| Продукт / заказчик | | | |

---

## 9. После релиза (follow-up)

- [ ] Мониторинг ошибок и алертов первые **N** часов  
- [ ] Обновление runbook при отклонениях  
- [ ] Закрытие релизного тикета и тег в VCS  

---

*Инструкция: для каждого релиза копируйте этот файл в `docs/releases/YYYY-MM-DD_vX.Y.Z_readiness.md` (или свой каталог), заполняйте и храните вместе с тегом/образом.*
