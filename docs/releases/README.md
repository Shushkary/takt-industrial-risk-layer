# Releases Folder

This folder stores per-release readiness cards created from `docs/release_readiness_template.md`.
See `INDEX.md` for the current release bundle map.

Recommended naming:

- `YYYY-MM-DD_vX.Y.Z_readiness.md`

Each file should be filled before deployment and linked to the final build/tag used in the target environment.
