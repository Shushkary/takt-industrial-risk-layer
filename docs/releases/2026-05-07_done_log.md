# Done Log — 2026-05-07

Черновой журнал выполненных работ для переноса в релизную readiness-card.

## Функциональные и архитектурные блоки

- Реализован remediation execution journal:
  - расширенная модель попытки (`action`, `result`, `readiness_before`, `readiness_after`);
  - API записи/повторной проверки readiness/истории recheck с пагинацией.
- Усилен PDF evidence:
  - SHA-256 и timestamp экспорта сохранены в кейсе;
  - проброс в SIEM/forensic payload и HTTP headers.
- Forensic root hash signature:
  - поддержка `unsigned_mvp`, `hmac_sha256_mvp`, `external_qualified_detached`, `external_gost2012_detached`;
  - внешний HTTP signer/verifier адаптер через `TAKT_FORENSIC_*`.
  - strict readiness gate (`TAKT_FORENSIC_CRYPTO_MODE=gost_strict`) через `/ready` и health-поля `forensic_*`;
  - в strict-контуре HMAC не используется как fallback для `signature_status`.
- Audit engagement workflow:
  - API `/audit-engagements*` (create/list/get/advance/findings/final-report);
  - экспорт `GET /audit-engagements/{id}/export/report.json`;
  - интеграция в forensic bundle по `engagement_id` (`engagement.json`, `engagement-report.json`).
- GosSOPKA:
  - валидация payload;
  - transport-envelope endpoint (`/export/gossopka-transport.json`);
  - official-format экспорт для smoke/`release_finalize` (`/export/gossopka-official-transport.json`).
- Audit immutability:
  - append-only `case_audit_ledger`;
  - append-only `operation_audit_ledger` (decision/import streams);
  - API verify endpoints и CLI verify scripts.

## Storage / migrations / scripts

- SQLite schema доведена до версии 6.
- `scripts/db_migrate.py` и `scripts/db_backup.py` покрыты тестами.
- Добавлены verifier scripts:
  - `scripts/verify_audit_ledger.py`
  - `scripts/verify_operation_ledger.py`

## Observability / release gates

- Добавлены monitoring artifacts:
  - Grafana dashboard JSON
  - Prometheus alert rules YAML
- CI `release-gates`: CycloneDX SBOM (генерация + artifact check), monitoring artifacts gate, ledger verifier fixture + CLI (`verify_*_ledger`), `pip-audit`, Schemathesis, mutation gate (`weights_loader.py`).
- CI `release-evidence-dry-run`: `release_finalize.py` → локальный API smoke (`close_operational_tails`, флаги `gossopka_official_ok`, forensic, `audit_engagement_api_ok`), затем `build_release_package.py` и `verify_release_package.py`.

## Documentation consistency

- Добавлены/обновлены release-документы:
  - `docs/release_checklist.md`
  - `docs/release_readiness_status.md`
  - `docs/release_readiness_template.md`
  - `docs/current_operational_reference.md`
  - `docs/releases/README.md`
- Убраны устаревшие ссылки на `sbom.cdx.json` / `sbom-components.json`.
- Убраны machine-local absolute paths из docs/README.

## Line endings / editor defaults

- Добавлены:
  - `.gitattributes` (LF normalization)
  - `.editorconfig` (LF + базовые редакторные правила)

## Финальная валидация

- Полный прогон тестов: `600 passed, 1 skipped`.
