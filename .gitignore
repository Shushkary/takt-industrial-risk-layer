# SQLite (storage.backend: sqlite)
/data/
*.db
*.sqlite
*.db-wal
*.db-shm

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
node_modules/
.pytest_cache/
.pytest_tmp_path_local/
.hypothesis/
.import_linter_cache/
.tmp_pytest*/
tmp_pytest*/
.tmp_pip_audit_cache/
.coverage
htmlcov/
dist/
*.egg-info/

# Local env (secrets); keep .env.example tracked
.env

# IDE
.idea/
.vscode/

# Разбор продуктов Positive Technologies и стратегия отстройки. Публикация в
# репозитории, который читают эксперты PT, — решение владельца, не техническое.
# Файл уже попадал в коммит случайной командой `git add -A docs`.
docs/pt_techlab/sprint_plan_v2_pt_aligned.md

# Локальные разрешения агента: содержат адрес и учётную запись боевой ВМ, а репозиторий
# публичный. Проектный `.claude/settings.json` при необходимости коммитится осознанно —
# в него адреса и учётные записи не кладём.
.claude/settings.local.json
