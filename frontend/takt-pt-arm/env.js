// Параметры контура, в котором работает рабочее место. Отдельный файл, а не строка в
// разметке: политика безопасности страницы запрещает встроенные скрипты (`script-src 'self'`),
// и такой блок браузер просто не выполнит.
//
// База документации. Пояснения в окнах ссылаются на документы репозитория (поле `doc` в
// реестре `HELP`), и без базы ссылка разрешается относительно страницы: на боевом контуре
// `try_files` отдаёт на такой путь `index.html` с кодом 200, то есть аналитик молча
// возвращается на ту же страницу и читает это как несработавшую кнопку.
//
// Значение указывает на публичный репозиторий: пути реестра совпадают с путями репозитория
// один в один, документ всегда соответствует выложенному коду, и поддерживать вторую редакцию
// документации не нужно. Чтобы читать документы локально — из каталога `docs/`, который
// раздаёт `scripts/serve_pt_arm.py`, — замените значение на './'.
window.TAKT_DOCS_BASE = 'https://github.com/Shushkary/takt-industrial-risk-layer/blob/main/';
